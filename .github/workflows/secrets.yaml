name: secrets
on:
    push:
        branches: [main]
    pull_request:
        type: [opened, reopened, synchronize]
    workflow_dispatch:
jobs:
    fetch-secrets:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: echo this is my secret ${{secrets.DB_PASSWORD}}
            - run: npm install
            - run: |
                  API_TOKEN=$(node cli.js rdstr)
                  echo this is my token $API_TOKEN
